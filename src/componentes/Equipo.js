import React, { Component } from 'react';
import {LaligaContext} from './Liga';

class Equipo extends Component {
    render() { 
        return ( 
            <LaligaContext.Consumer>
                {(value) =>{

                }}
            </LaligaContext.Consumer>
         );
    }
}
 
export default Equipo;